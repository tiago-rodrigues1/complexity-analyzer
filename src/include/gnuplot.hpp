#ifndef GNUPLOT_HPP
#define GNUPLOT_HPP

#include <vector>
#include <string>

// Apenas a declaração da função vai aqui.
void plot_graph(const std::vector<long long>& x_data, const std::vector<double>& y_data);

#endif // GNUPLOT_HPP